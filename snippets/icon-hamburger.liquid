{%- liquid
  assign size_class = size | default: 'size-6'
-%}
<svg
        class="{{ size_class }}"
        viewbox="0 0 100 80"
>
    <rect height="20" width="100"></rect>
    <rect
            height="20"
            width="100"
            y="30"></rect>
    <rect
            height="20"
            width="100"
            y="60"></rect>
</svg>