<div
        class="w-72 p-5 bg-white rounded-md shadow-2xl"
        data-ajax-cart-section
        x-cloak>
    <div class="flex items-center justify-between mb-2.5">
        <h2 id="minicart-label" class="text-blue-500 text-2xl font-bold m-0">
            Cart
        </h2>
        <button class="bg-transparent border-0 cursor-pointer text-gray-500 hover:text-gray-600 focus:outline-none focus:ring-2 focus:ring-gray-500" @click="$store.global.isMinicartVisible = false">
            {% render 'icon-close-x', size: 'size-8' %}
        </button>
    </div>
    <div class="mb-2.5 text-sm text-gray-700">
        You have
        <span
                class="font-semibold"
                x-data>{{ cart.item_count }}
    </span>
        <span x-data></span>
        in your cart!
    </div>
    <hr class="mb-5 border-gray-200"/>
    <ul class="list-none p-0 max-h-[300px] overflow-y-scroll" data-ajax-cart-section-scroll>
        {% for line_item in cart.items %}
            <li class="flex mb-2.5 w-full">
                <div class="w-[100px] h-[120px] relative flex-shrink-0">
                    <img
                            src="{{ line_item.image | image_url: width: 100, height: 120, crop: 'center' }}"
                            alt="{{ line_item.title }}"
                            srcset="{{ line_item.image | image_url: width: 100, height: 120, crop: 'center' }} 1x, {{ line_item.image | image_url: width: 200, height: 240, crop: 'center' }} 2x"
                            width="{{ line_item.image.width }}"
                            height="{{ line_item.image.height }}"
                            loading="lazy"
                            class="absolute inset-0 h-full w-full object-cover object-center"
                    >
                </div>
                <div class="ml-2.5 flex flex-col justify-start">
                    <h4 class="text-sm font-semibold mt-0 mb-1.5 text-gray-900">
                        {{ line_item.title | truncatewords: 5 }}
                    </h4>
                    <div class="my-0.5">
                        {% for option in line_item.options_with_values %}
                            <p class="m-0 mb-0.5 text-xs text-gray-600" data-option-name="{{ option.name }}">
                <span class="font-semibold">{{ option.name }}:
                </span>
                                <span>{{ option.value }}
                </span>
                            </p>
                        {% endfor %}
                    </div>
                    <div class="my-0.5 text-xs text-gray-700">
            <span class="font-bold">Quantity:
            </span>
                        <span>{{ line_item.quantity }}
            </span>
                    </div>
                    <p class="mt-auto mb-0 text-sm font-medium text-gray-900">{{ line_item.price | money }}
                    </p>
                    <p class="text-[0.6em] mt-1 mb-0">
                        <a href="{{ line_item.url_to_remove }}" data-ajax-cart-request-button class="inline-flex items-center gap-1 text-red-600 hover:text-red-700 hover:underline focus:outline-none focus:ring-2 focus:ring-red-400">
                            {% render 'icon-trash' %}
                            Remove from cart
                        </a>
                    </p>
                </div>
            </li>
        {% endfor %}

    </ul>
    <hr class="my-5 border-gray-200"/>
    <div class="flex items-center justify-between">
        <p class="h-4 m-0 text-gray-700">Subtotal
        </p>
        <p class="font-semibold text-xl m-0 text-gray-900">{{ cart.items_subtotal_price | money }}
        </p>
    </div>
    <a class="flex justify-center items-center mt-5 w-full h-[43px] font-semibold bg-white text-gray-900 border-2 border-gray-900 hover:text-white hover:bg-gray-900 transition-colors focus:outline-none focus:ring-2 focus:ring-gray-500 no-underline" href="{{ routes.cart_url }}">
        View Cart Details
    </a>

</div>